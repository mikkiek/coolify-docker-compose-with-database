services:
  app:
    image: node:20-slim
    container_name: app
    working_dir: /app
    command: sh -c "npm install && node server.js"
    volumes:
      - .:/app
    restart: always
    labels:
      - "coolify.managed=true"
